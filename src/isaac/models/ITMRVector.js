export default class ITMRVector {

  /**
   * Create Vector for Isaac On Twitch mod
   * @param {Number} x - X coordinate
   * @param {Number} y - Y coordinate
   */
  constructor (x, y) {
    this.X = x;
    this.Y = y;
  }

}